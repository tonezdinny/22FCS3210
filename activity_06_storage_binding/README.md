Consider the [Die.java](src/Die.java) class and classify each of its variables (named or unnamed) according to their storage binding as:

* static
* stack-dynamic
* explicit heap-dynamic
* implicit heap-dynamic
